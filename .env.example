# PostgreSQL - Opción 1: URL (si la contraseña tiene caracteres raros, usa Opción 2)
# DATABASE_URL=postgresql://usuario:password@localhost:5432/cash-up

# Opción 2: Variables sueltas (recomendado si la contraseña tiene @, #, :, etc.)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=tu_password
DB_NAME=cash-up

NODE_ENV=development
